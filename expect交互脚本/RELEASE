## 1.2 更新

1. 移除expect脚本本身的日志, 改由主调脚本输出. expect脚本通过返回状态码告知主调脚本ssh登录结果

2. 可以在ssh登录后执行指定命令, 并记录执行结果

3. 修改主调脚本中按行读取IP列表内容的方式. while read 方法由于标准输入会干扰到expect脚本的运行, 而for..in..方法又会将空格视为分隔符, 所以更换成用sed读取指定行的内容, 使用line_num变量作为计数器.